# macOS junk
.DS_Store
._*

# Claude Code local settings (keep shared config, ignore personal/local)
.claude/settings.local.json
.mcp.json

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Data / model artifacts (大文件与生成物,避免污染版本库)
/data/
*.parquet
*.feather
checkpoints/
*.ckpt
*.safetensors
.superpowers/

# run-store(compare 运行结果落盘,YOUZI_RUNS_DIR 默认目录)
runs/

# US rebuild
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
/snap/
/runs/
*.parquet

# Verdict run artifacts (PIT stores + rendered outputs — regenerable, keep local, never commit)
/verdict_pit*/
/verdict_*.json
/verdict_universe.txt

# Secrets / local credentials — NEVER commit (Alpaca + LLM keys)
.env
.env.*
*.env

# Live brain + teaching sessions (regenerable local state)
/state/

# Hermes vendor-feasibility spike — gitignored clone
spikes/2026-06-26-hermes-vendor-feasibility/_hermes/
.pytest-cache/

# scratch / run artifacts (CLAUDE.md §7)
decisions/
deck.png
.playwright-mcp/
.codex/
