"""AQR evaluation entry point; initialization only, no implementation yet.

The existing repository PIT beds do not cover this assignment. The dsh
engineering task must implement the frozen protocol using an external
historical source and replay_days(source=..., start=..., end=...).
"""

if __name__ == "__main__":
    raise SystemExit(
        "AQR pipeline is not implemented yet. Read protocol.md; do not run the "
        "template PIT backtest or report its output as assignment performance."
    )
