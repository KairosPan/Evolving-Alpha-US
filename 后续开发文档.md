# 自进化游资系统 · 后续开发文档(Development & Handoff Guide)

> 给下一位开发者(或下一个会话)的接力文档。**先读这三份**:本文(怎么继续)· `PROJECT_STATE.md`(一页纸状态)· `自进化游资系统-架构蓝图-v1.0.md`(权威设计)。
>
> 截至:2026-06-08 · `main` 含 Phase-1c-PIT + FE-0/FE-B/FE-A(web 地基 + 研究/决策驾驶舱)· 299 测试全绿 · 离线无网络依赖。

---

## 0. 一分钟上手

```bash
cd "/Volumes/kairos/引力场量化/youzi-自进化版"
python3 -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"          # akshare/pandas/pydantic/pyarrow/openai/pytest
python -m pytest -q              # 284 passed,全部离线(FakeSource/MockLLM/TestClient,不触网)
python scripts/serve_web.py      # 起 web(FE-0):http://127.0.0.1:8000 → research/H 查看器
```
- 代码在 `youzi/`(9 个包)。测试在 `tests/`。计划在 `docs/superpowers/plans/`。种子在 `seeds/`。
- **触网只有两处**,都只在手动 smoke 脚本里:`scripts/smoke_akshare.py`(真实行情)、`scripts/smoke_deepseek_agent.py`(真实 DeepSeek,需 `DEEPSEEK_API_KEY`)。CI/测试永不触网。

---

## 1. 现状(已建成)

一句话:论文《Continual Harness》的两环自进化 substrate 已搭好,**act 半环(LLM Agent)已通**,refine 半环(1b-1 观测层 + 1b-2 LLM Refiner)已并入,**1b-3a 内环编排**把它跑成交错自进化环(`InnerLoop.run()`:每日 act(live H)→延迟打分→在线信用→能力地板熔断→每日 refine,reset-free),**1b-3b 度量对比**把它放上同窗同 oracle 的尺(`compare_harnesses`:HCH 自精炼 vs Hexpert 种子静态 vs Hmin 裸 baseline + `hch_beats_hexpert` verdict)。**内环 + 度量机器全就绪;真实"自进化是否胜 frozen"待真实 DeepSeek 多日跑验证**(MockLLM 忽略提示,测不了 refine 实效)。下一步可选 1b-3c(影子 Hexpert 环内严格地板)或直接 1c 协同学习外环。

| 阶段 | 内容 | 包 | 状态 |
|---|---|---|---|
| Phase-0a | 数据·特征·**reset-free 回放·未来函数防火墙** | `data` `features` `replay` `schemas` | ✅ |
| 种子 v1 | 轮回 playbook → 结构化 K/M/p(57技能/21记忆/22doctrine/7相位) | `seeds/` | ✅ |
| Phase-0b-1 | Harness 状态层:载入+查询 | `harness` | ✅ |
| Phase-0b-2 | 可编辑 Harness:9 meta-tools + immutable 写保护 + EditLog 审计 | `harness` | ✅ |
| Phase-0b-3 | 持久化:序列化 + 版本化快照 + 回滚 | `harness` | ✅ |
| Phase-0c | 候选 universe / 个股快照层 | `universe` | ✅ |
| Phase-0d | 评测脚手架 + **已实现未来 oracle**(交易版 Dijkstra) | `eval` | ✅ |
| Phase-1a | **LLM Agent(act 半环)** | `llm` `agent` | ✅ |
| Phase-1b-1 | **轨迹 + 信用分配 + 失败签名(确定性观测层,无 LLM)** | `eval` `refine` | ✅ |
| Phase-1b-2 | **LLM Refiner(读证据→4-pass CRUD 经 meta-tool 编辑 H)= 真正的自进化** | `refine` `llm` `agent` `harness` | ✅ |
| Phase-1b-3a | 内环编排(交错 act→打分→在线信用→每日 refine,reset-free)+ 能力地板熔断 | `loop` `refine` | ✅ |
| Phase-1b-3b | 度量对比(HCH/Hexpert/Hmin 同窗同 oracle + verdict) | `loop` | ✅ |
| Phase-1b-3d | Refiner 纪律化(退役证据门 n≥K + faded≠nuked 提示纪律,治退化根因) | `refine` | ✅ |
| Phase-1b-3e-1 | OHLCV 数据 + 前向收益 oracle(次日开盘买→t+N收盘卖;纯计算,不接入) | `data` `eval` | ✅ |
| Phase-1b-3e-2 | 收益打分接入(可插拔 Scorer:PoolScorer 默认/ReturnScorer;apply_credit 用 sc.score) | `eval` `refine` `loop` | ✅ |
| **真实收益对比** | `smoke_compare --scorer return` horizon=2,看收益信号是否让 HCH 产 alpha | (人工) | ⏭ 下一步 |
| **Phase-1b-3c** | 影子 Hexpert 环内严格地板(并行冻结 agent 实时熔断) | (待建) | ⏭ 可选 |
| Phase-1c | 协同学习外环(PRM+teacher relabel+LoRA) | (待建) | 之后 |

**模块地图**(`youzi/`,40 个模块):
- `data/`:`source`(MarketDataSource 协议 + AkshareSource[含 `_retry_ak` 重试] + GuardedSource + `daily_ohlcv`)· `calendar` · `cache`(`PITStore`:per-(kind,day)池 + per-code OHLCV + calendar,**原子写**)· **(1c-PIT)** `snapshot_source`(`SnapshotSource` 离线读 store、6 方法、池缺报错/OHLCV缺返空/date归一)· **(1c-PIT)** `capture`(`capture_window` 一次性节流预取 akshare→store,幂等续跑+ValueError容错)
- `features/`:`echelon`(连板梯队)· `blowup`(炸板率)· `money_effect` · `sentiment`(regime-relative)· `builder`(组装 MarketState)
- `replay/`:`firewall`(AsOfGuard/LookaheadError)· `engine`(ReplayEngine,reset-free)
- `schemas/`:`market`(MarketState/EchelonRung,frozen)
- `harness/`:`regime`(相位归一,1b-2 加非字符串类型安全守卫)· `skill`(`SkillStats` 加 `nukes`;`registry._PATCH_FORBIDDEN` 加 `stats` 写保护)· `memory_item` · `doctrine` · `cycle`(状态机)· `registry` · `memory_store`(1b-2 加 `_UPDATE_FORBIDDEN={importance}`)· `harness`(HarnessState)· `loader`(载入 seeds)· `errors` · `edit_log`(`EditRecord` 加 `rationale`)· `metatools`(9 meta-tools,加 rationale 透传)· `snapshot`(磁盘版本)· `manager`(checkpoint/rollback)
- `universe/`:`stock`(StockSnapshot)· `universe`(CandidateUniverse + build_universe)
- `eval/`:`decision`(Candidate/DecisionPackage/DecisionPolicy)· `oracle`(已实现未来,池成员制)· `metrics`(EvalReport)· `baselines`(NoTrade/HighestBoard)· `walk_forward`(延迟打分;加 `walk()→Trajectory`、`report_from_trajectory`)· `trajectory`(**1b-1**:Trajectory/TrajectoryStep/EntrySnap)· **(1b-3e-1)** `return_oracle`(`forward_return` 纯函数 + `ReturnOracle`:次日开盘买→t+N收盘卖收益幅度;`data/source.daily_ohlcv` 取数)· **(1b-3e-2)** `scorer`(可插拔 `Scorer`:`PoolScorer` 默认/池成员制 + `ReturnScorer` 收益打分缺收益丢弃;`walk_forward`/`inner_loop`/`compare` 注入;`apply_credit` 改用 `sc.score`)
- `refine/`:`credit`(`apply_credit`:oracle→SkillStats,`CreditReport`/`SkillCredit`,`unattributed` 桶,Welford 期望;**1b-3a** 加 `merge_credit_reports` 只读合并)· `signatures`(`extract_signatures`:board-rank×outcome 四类入场失败签名)· **(1b-2)** `ops`(`RefineOp`/`PASS_TOOLS`/`parse_ops`)· `refiner_prompt`(各 pass 系统提示 + 证据 user 提示)· `refiner`(`Refiner.refine` 4-pass CRUD + 拒绝管线 + `RefineReport`)
- `llm/`:`client`(LLMClient/MockLLMClient/DeepSeekClient,1b-2 加 retry)· **(1b-2)** `extract`(`extract_json_object` 配平括号)
- `agent/`:`prompt`(H→提示,1b-2 注入技能战绩+win 记忆)· `parse`(LLM→DecisionPackage,幻觉过滤;1b-2 复用 extract)· `agent`(LLMAgentPolicy)
- `loop/`(**1b-3a 新包**):`inner_loop`(`LoopConfig`/`RefineEvent`/`BreakerEvent`/`LoopReport` + `InnerLoop.run()`:交错 act→延迟打分→在线信用→能力地板熔断→每日 refine,reset-free;rollback 后 `_rebind`)· **(1b-3b)** `compare`(`ArmReport`/`ComparisonReport` + `compare_harnesses`:HCH/Hexpert/Hmin 四路同窗同 oracle 对比 + delta/verdict,factory 注入)

---

## 2. 核心不变量与约定(动手前必须知道)

这些贯穿全代码,新增任何东西都要遵守。多次终审就是在守这些。

1. **未来函数防火墙(头号铁律)**。任何"决策时刻"的代码只能用 ≤t 的信息。
   - 取数一律经 `GuardedSource`(`AsOfGuard.check(day)`,请求未来日 → `LookaheadError`)。
   - 评测打分用**延迟打分**(`WalkForwardEval`):t 决策、游标自然推进到 t+horizon 才打分。
   - "已实现未来"oracle/teacher 的未来标签**只进训练/打分,永不进决策推理路径**。
   - 防御还来自**结构**:`MarketState`/`StockSnapshot`/`CandidateUniverse` 是 frozen、无 source 句柄的快照——拿到它的策略**够不到**任意日期。
2. **Harness `H=(p,G,K,M)`**:`p`=doctrine(`Doctrine`)· `K`=技能(`SkillRegistry`)· `M`=记忆(`MemoryStore`)· 情绪周期状态机(`StateMachine`)。G(子 Agent)= Phase-1 的 LLM 模块,目前只有主 Agent。**结构性编辑(改打法)只能经 `MetaTools` 的 9 个 meta-tool**(write/patch/retire→dormant/revive/promote_skill、process/update/demote_memory、rewrite_doctrine),且进 `EditLog` 审计。
   - **观测 vs 编辑边界(1b-1 确立)**:`SkillStats`(n/wins/losses/nukes/ewma/expectancy)是**观测**——`refine.apply_credit` 按已实现 oracle 结果**直写**,不算结构性编辑、**不入 EditLog**;`EditLog` 只记 Refiner 的结构性 Δ。stats 仍被快照捕获,rollback 覆盖。
3. **immutable-core 写保护**:`DoctrineEntry.immutable=True` 的纪律红线,`__setattr__` 守卫 + `Doctrine.rewrite/remove` 双重拦截,Refiner 永远改不动。
4. **已实现未来 = 交易版 Dijkstra**(`eval/oracle`):pool-membership 类别(次日仍涨停=continued / 跌停炸板=nuked / 掉出=faded)。这是量化"决策好不好"的客观尺。**策略(含 LLM Agent)都是 `DecisionPolicy`,统一进 `WalkForwardEval` 量化。**
5. **代码约定**(照抄现有风格):
   - PIT 快照模型 = **frozen pydantic**(`ConfigDict(frozen=True)`);种子载入模型 = **`extra="forbid"`**(catch 拼写错);可被 patch 的 = **`validate_assignment=True`**。
   - 容器类(SkillRegistry/MemoryStore/CandidateUniverse/EditLog)= **`__bool__ = True`**(杀 falsy-empty 陷阱,曾两次中招)。
   - **缺失值诚实 `None`,不臆造 0/""**;`pd.isna(v)` 判缺(含 NaT)。
   - **regime 归一**:`regime.split_regimes`/`parse_regime_field` 把不统一的相位词归一到 7 个 canonical 相位(`混沌冰点/修复启动/情绪回暖/题材启动/主升/震荡补涨/退潮`)+ 生态标签。
   - LLM 输出**一律不可信**:`agent/parse` 过滤幻觉 code(只留候选池内的)、钳 confidence、malformed→空仓兜底。
6. **离线可测**:测试用 `FakeSource`(行情)/ `MockLLMClient`(LLM),永不触网。真实 akshare/DeepSeek 只在 `scripts/smoke_*.py`(手动)。

---

## 3. 开发工作流(本项目一直这么干,建议沿用)

每个阶段一个完整循环,产出可工作、可测、已评审的软件:

1. **写计划**(`superpowers:writing-plans`):落到 `docs/superpowers/plans/YYYY-MM-DD-<topic>.md`,bite-sized TDD 任务、精确路径/完整代码/命令、自检(spec 覆盖/无 placeholder/类型一致)。**范围切到"一个可独立测试的单元"**,大了就拆(如 Phase-0b 拆 1/2/3)。
2. **subagent-driven 执行**(`superpowers:subagent-driven-development`):
   - 把任务**按层 bundle**(3-4 个),每 bundle 派一个 implementer 子 Agent(TDD、per-task commit)。
   - 每 bundle **两段评审**:① spec 合规(对照计划逐行)② 代码质量(对抗性找真问题)。评审发现 → 同一 implementer 修 → 复核。
   - 全部 bundle 完 → **opus 终审**(集成级:不变量、防火墙、真实数据鲁棒性)。
3. **收尾**(`superpowers:finishing-a-development-branch`):feature 分支 → 验证 merged main 全绿 → 合并(FF)→ 删分支。每阶段更新 `PROJECT_STATE.md` + memory。
- **git**:`main` 受保护,功能在 `phase-xx` 分支做;commit message 中文、按 feat/fix/refactor/test/docs/chore 分类。
- 评审拦下的真问题样本(说明评审值):falsy-empty-log 静默丢弃、`__setattr__` 绕过 immutable、NaT→"None" 字符串、半改污染、幻觉 code、未来函数侧信道泄漏。**这些都不是走过场。**

---

## 4. 路线图与下一步

### ▶ Phase-1b:Refiner(真正的自进化,最高优先)—— 拆 1b-1/1b-2/1b-3

**✅ 1b-1(已并入 main):确定性、无 LLM 的"观测层"。** spec/plan 在 `docs/superpowers/{specs,plans}/2026-06-01-phase1b1-...`。给了:
- `Trajectory`(frozen,记 `(date, market, decision, 入场上下文 entries, 延迟 outcomes)`;尾部不足 horizon 的步显式 `scored=False`)+ `WalkForwardEval.walk()→Trajectory`(复用唯一延迟打分循环,`run()` 行为不变,等价性测试守既有)。
- `apply_credit(traj, h)`:把已实现 oracle 结果归因到 agent 引用的技能,填 `SkillStats`——`continued`=win,`faded`/`nuked`=非 win 但 `expectancy` 用 Welford 保留 0/−1 区别,`nukes` 单列;`pattern→技能` resolve,未匹配进 `unattributed` 桶不静默丢。
- `extract_signatures(traj, h)`:board-rank×outcome 四类**入场**失败签名(`chased_into_nuke`/`weed_over_dragon`/`generic_nuke`/`faded_miss`)。
- **观测 vs 编辑边界**(见 §2.2)。

**✅ 1b-2(已并入,自进化闭环结构上成立):LLM Refiner。** spec/plan 在 `docs/superpowers/{specs,plans}/2026-06-06-phase1b2-...`。`Refiner.refine(traj, credit, signatures) → RefineReport`:读证据 + 当前 H(含已填 stats)→ **论文式 4-pass CRUD**(Δp→ΔG→ΔK→ΔM,reset-free 顺序、就地编辑同一 H、agent 立即可见)→ 逐条经 `MetaTools` 应用进 `EditLog`(带 `rationale`)。**已锁定决策**(brainstorm 确认):
  - **结构 = 论文式 4-pass**(非单次调用);**ΔG = 占位 no-op**(G 子 Agent 未建,不发 LLM 调用,记 "G-pass reserved";现 3 次 live 调用 p/K/M,G 落地即插)。
  - **编辑面 = 全 9 个 meta-tool** + per-pass/per-refine 编辑上限 + rationale 必填;拒绝管线对 immutable/非法转移/越权/缺rationale/超上限/幻觉target/malformed/重复id **全拒、绝不半应用/不崩**(异常→`RejectedEdit`)。
  - **范围 = 相邻三项全做**:① `DeepSeekClient` 指数退避重试(可注入 sleep,离线可测)② agent 系统提示注入技能战绩 + win 记忆(闭合 1b-1→agent)③ 鲁棒共享 `extract_json_object`(配平括号,`parse_decision` 复用)。
  - **opus 终审硬化(5 视角对抗 + 真实种子端到端 + 逐条裁定 → 抓出并修复 4 类 LLM-不可信边界 blocking)**:① **观测字段写保护**——`SkillStats`/`Lesson.importance` 是观测,不可经 `patch_skill`/`update_memory` 改(`_PATCH_FORBIDDEN`/`_UPDATE_FORBIDDEN`),create 时(`write_skill`/`process_memory`)剥离注入值;② **非字符串 regime 类型安全**(`regime.py` isinstance 守卫 + `_apply_op` 捕获 AttributeError,不再崩 refine);③ **write_skill 状态钳制为 incubating**(防 LLM 直接铸造 active 技能绕过孵化→晋升闸)。
  - 重点价值兑现:`write_skill` 把"防坑结构 codify 成 failure_detector 技能"、`retire/revive` 杀亏的/复活休眠的、`rewrite_doctrine` 改 mutable 纲领、`process_memory` 从失败写教训;**immutable 红线改不动(底座 + 拒绝管线双保险)**。217 测试绿(全离线 MockLLM + 真实种子端到端)。

**✅ 1b-3a(已并入 main):内环编排。** spec/plan 在 `docs/superpowers/{specs,plans}/2026-06-06-phase1b3a-...`。`youzi/loop/inner_loop.py` 的 `InnerLoop.run()→LoopReport`:`act(live H)→延迟打分→在线信用(每步 apply_credit 恰一次)→能力地板熔断→每日 refine(有评分证据才触发:checkpoint-before + Refiner.refine,证据用 merge_credit_reports 只读合并)→ 下一日`,reset-free 单游标单 live H,**Refiner 编辑次日 agent 立即可见**。能力地板熔断 = 自相对 baseline + 绝对 floor_abs → 触发后 `rollback` 上个 checkpoint + `_rebind`(解 0b-3 旧引用债务)+ 冻结后续 refine(无 checkpoint 则只冻结)。opus 终审 0 blocking(防火墙/在线信用不重复/reset-free+rebind 三视角 PASS + 真实种子端到端环测试),修 2 minor(LoopConfig Field 校验、refine 仅在有证据时触发)。230 测试绿。

**✅ 1b-3b(已并入 main):度量对比。** spec/plan 在 `docs/superpowers/{specs,plans}/2026-06-06-phase1b3b-...`。`youzi/loop/compare.py` 的 `compare_harnesses(harness_factory, source, start, end, *, agent_llm_factory, refiner_llm_factory, store_factory, ...)→ComparisonReport`:四路同 source/区间/horizon/同 oracle 对比 `HCH`(InnerLoop 自精炼,`report_from_trajectory(LoopReport.trajectory)`)vs `Hexpert`(冻结种子 H + agent,`walk()`,**无 Refiner**)vs `Hmin_highest`(HighestBoard)vs `Hmin_notrade`(NoTrade);**factory 注入**让每路独立 fresh 种子 H + 独立 LLM client(防交叉污染);出 `HCH−Hexpert` delta + `hch_beats_hexpert` verdict + HCH 环信息。opus 终审 0 blocking、0 confirmed minor(真实种子四路端到端通过)。236 测试绿。**⚠ MockLLM 忽略提示 → 测的是对比机器,真实"自进化是否胜 frozen"需真实 DeepSeek 多日跑。**

**✅ 1b-3d(已并入 main):Refiner 纪律化。** spec/plan 在 `docs/superpowers/{specs,plans}/2026-06-07-phase1b3d-...`。治真实数据退化根因(Refiner 小样本过度退役):① **退役证据门**——`Refiner._apply_op` 对 `retire_skill`,`skill.stats.n < RefinerConfig.min_retire_samples`(默认 5,`Field(ge=1)`)即拒绝(只读 stats、dispatch 前 return 不半应用、技能不存在仍走 KeyError、permanent 同门);② **K-pass 提示纪律**(注入真实门槛 + faded≠nuked + 能 patch 别 retire);③ 顺修 `_target_id` 归一 `str|None`(LLM 发数字 id 不崩拒绝路径)。门是 Refiner 策略(MetaTools/registry 未动)。opus 终审 0 blocking。246 测试绿。**⚠ 仍待真实三窗复测**验证纪律是否纠正 HCH 退化。

**✅ 1b-3e-1(已并入 main):OHLCV 数据 + 前向收益 oracle。** spec/plan 在 `docs/superpowers/{specs,plans}/2026-06-08-phase1b3e1-...`。`data/source.daily_ohlcv`(协议/Akshare qfq/Guarded 守 `end≤as_of`)+ `eval/return_oracle`:`forward_return(ohlcv, entry_day, exit_day)` 纯函数(次日开盘买→t+N 收盘卖,缺数诚实 None)+ `ReturnOracle`。**纯计算+取数,不接入打分链路。** 254 测试绿,opus 终审 0 blocking。

**✅ 1b-3e-2(已并入 main):收益打分接入。** spec/plan 在 `docs/superpowers/{specs,plans}/2026-06-08-phase1b3e2-...`。`eval/scorer.py` 可插拔 `Scorer`:`PoolScorer`(默认=池成员制 SCORE,回归安全)+ `ReturnScorer`(`score`=前向收益、`outcome` 仍池类别、**缺收益候选丢弃**),打分块抽成 `score_step` 由 `WalkForwardEval`/`InnerLoop`/`compare_harnesses` 注入(四路同 scorer)。`apply_credit` 改用 `sc.score`(池下零回归)→ 配 ReturnScorer 时 `mean_score`/`expectancy`=平均收益。`smoke_compare --scorer pool|return`。262 测试绿,opus 终审 0 blocking、1 minor(熔断阈值按池 SCORE 标定、ReturnScorer 须重标定=债务)。**真实收益跑被 akshare 限流挡住(findings §10)→ 做 1c-PIT。**

**✅ 1c-PIT(已并入 main):PIT 数据快照 + 离线打分。** spec/plan 在 `docs/superpowers/{specs,plans}/2026-06-08-phase1c-pit-snapshot...`。唤醒休眠 `PITStore` + 扩展(OHLCV per-code/calendar/**原子写**)+ `SnapshotSource`(离线读、6方法、池缺报错/OHLCV缺返空/date归一)+ `capture_window`(节流幂等续跑、ValueError容错)+ `capture_window.py` 脚本 + smoke `YOUZI_SNAPSHOT` 离线分支。防火墙原样(经 GuardedSource)、打分链路零改。277 测试绿,opus 终审 0 blocking、3 minor(原子写/OHLCV容错/越界测试,**均已修**)。**⏭ 下一步(人工)= 离线跑真实收益对比**:① `python scripts/capture_window.py <s> <e> snap`(建库一次,慢、节流、撞限流就重跑[幂等续跑])→ ② `YOUZI_SNAPSHOT=snap DEEPSEEK_API_KEY=... python scripts/smoke_compare.py <s> <e> 2 0.0 return`(离线无限次跑,akshare 出局),看更细信号是否让 HCH 产 alpha,记 findings。**🚧 建库被 akshare OHLCV 端点硬拒连挡(findings §11,探针0/5)→ 待 off-peak。债务:OHLCV 多源 fallback(eastmoney→sina→tencent)。**

**✅ FE-0(已并入 main):web 平台地基。** 新增 `youzi_web/`(**新 web 层,sibling of `youzi/`,领域零改、单向依赖**;FastAPI+Jinja2+HTMX 纯 Python)。spec/plan 在 `docs/superpowers/{specs,plans}/2026-06-08-fe0-web-foundation...`。技术栈定 **FastAPI+HTMX+模块化功能模块**(为"以后长出新闻分类/Agent 编排");外壳 **C**(图标轨+子导航,brainstorm 用可视化辅助选定)。组成:`registry`(`Feature/SubNavItem`+`FEATURES` 注册表,**加功能=append 一个 Feature、外壳零改**)+ `app`(create_app、Jinja ChoiceLoader 多模板目录、home 落首个 enabled 子页)+ `data_access`(`harness_view` 算 hit_rate/nuke_rate、`seed_harness`/`snapshot_harness`)+ 首模块 `features/research`/**H 查看器**(读种子 H 渲染 57 技能/纪律/记忆,纯离线)+ `scripts/serve_web.py`(http://127.0.0.1:8000)+ 本地 vendor htmx。284 测试绿(TestClient 离线),opus 终审 0 blocking、2 minor(home 防占位404、`.status-dormant` CSS,**均已修**)。

**✅ FE-B(已并入 main):研究驾驶舱全量。** spec/plan 在 `docs/superpowers/{specs,plans}/2026-06-08-feb-research-dashboard...`。**run-store**(`youzi/loop/run_store.py` `RunStore`:save/list/load `ComparisonReport` JSON,原子写+逐文件守卫;治"compare 跑完即没")+ `scripts/sample_run.py`(离线造样本 run)+ smoke_compare 持久化 hook + `data_access.list_runs/load_run`(`YOUZI_RUNS_DIR` env 可覆盖)+ research **3 视图点亮**(三方对比/refine 时间线/trajectory,**一份 ComparisonReport 内嵌 hch_loop_report 驱动 3 视图**)+ 顶栏运行选择器 + 空态。294 测试绿,opus 终审 0 blocking、2 minor(list 逐文件守卫、模板守 hch_loop_report=None,**均已修**)。**看成品**:`python scripts/sample_run.py` → `serve_web.py` → research 四子页(H查看器/三方对比/refine/trajectory)。

**✅ FE-A(已并入 main):决策驾驶舱。** spec/plan 在 `docs/superpowers/{specs,plans}/2026-06-08-fea-decision-cockpit...`。新 `features/decision/`(🎯)+ `/decision/cockpit?run=&day=` —— **离线渲染**已存决策(run-store 的 trajectory step):市场态面板(+连板梯队)+ 排序候选卡(理由/置信/**join 技能的计划**:`data_access.skill_plan`→`resolve_skill` 折种子 H,join 不到降级)+ 结果 + 空仓(no_trade_reason)+ run/日期选择器。v1 只展示。299 测试绿,opus 终审 2 视角全 PASS、0 发现。**看成品**:🎯 决策驾驶舱。**⏭ 下一步**:live 按需跑(选今天→agent 对 SnapshotSource+LLM 跑,需 akshare 恢复)/ news / agents 模块。

**⏭ 1b-3c(可选):影子 Hexpert 环内严格地板。** 改 `InnerLoop` 环内并行跑冻结 agent + 影子相对熔断(HCH 滚动跌破 Hexpert margin → 熔断),论文式实时保护(1b-3a/3d 用自相对地板 + 退役门)。可选增强:friction 修复(update_memory 改 regime/重复 lesson_id)、限制性 patch 机械门、在线 EWMA decay 加权信用、rollback-to-last-good、checkpoint keep-last-K、按 regime 分层对比。**或直接做 Phase-1c 协同学习外环。** **强烈建议先**:用 `scripts/smoke_compare.py` 再跑真实三窗,确认 1b-3d 是否把 HCH 拉回 ≥ Hexpert。

### Phase-1c:协同学习外环
本地/开源模型 π_θ 在活精炼 H 内跑 rollout → PRM 过程奖励 → 低奖励窗口 frontier teacher + 已实现未来 oracle relabel → soft-SFT/LoRA。需 GPU + 可训练模型。蓝图 §6.6。

### 增强(可并行)
- **N 日窗口 / 收益幅度 oracle**(需日线 OHLCV 数据扩展);**regime 分层评测**(需把"判读相位"做成可校准的 `G_cycle` 分类器子 Agent)。
- **数据补强**:龙虎榜 / 题材线(concept 成分,**注意 PIT/幸存者**)/ 仓位·组合·相关性层 / fill-feasibility(涨停买不进)/ 公告·监管硬否决器 / 成本·滑点模型(蓝图 §6.1/6.7)。
- **非平稳六道闸**(蓝图 §8):regime 标签 + decay 加权 + dormant 复活 + alpha-decay 监控 + 能力地板回退 + OOS/影子盘。decay-weighted stats、dormant 复活的底座(SkillStats EWMA、status 生命周期)已就绪,待接入 Refiner。

---

## 5. 已知债务清单(汇总自各阶段终审,按优先级)

**P0(实盘/真数据前必须解决)**
- **真实列名核对**:跌停池封单列是 `封单资金`≠`封板资金`;zt 池可能 `最后封板时间`≠`首次封板时间`。跑 smoke 核对并修 `data/source.py` 的 `_RENAME`(离线测试测不到)。
- **幸存者偏差 / PIT 成分**:akshare 概念成分是当前成分;退市/ST/改名/当时可交易池未处理。**当前防火墙只防日期 lookahead,不防 survivorship**。需 point-in-time 数据治理 + 负例(看着像但失败了的)。
- ~~**DeepSeekClient 无 retry/backoff**~~ **已在 1b-2 解决**(指数退避,可注入 sleep);仍建议 1b-3 编排层在 `refine`/`decide` 外包 try/except 决定空仓/跳过当日。

**P1(Phase-1b 落地时处理)**
- 提示**全量注入**(55 技能+全 doctrine,~24KB/次)未按 `regime_read` 选择性注入(卡在缺 `G_cycle` 分类器);✅ `win` 类记忆 + 技能 `stats` **已在 1b-2 注入**(agent 能看到"这技能在亏/被砸");`oracle_gap` 仍未注入。
- `EditRecord.payload` 多数 op 只记新值无 before-image → 当前回滚靠整版快照(够用);若要 delta-replay 回滚需补 before-image。
- `MetaTools.h/log` 公开可绕审计(soft 边界);PITStore 已实现但**未接入取数路径**(应作 read-through 插进 GuardedSource 内层,实现"边跑边快照")。
- 评测:`HighestBoard` 平局选全部 → n_candidates 偏大,比 agent 时需归一;expectancy 是**毛收益**(无成本)。

**P2(增强 / 提升进化质量)**
- ~~`_extract_json` 对 thinking-blob/多 JSON 对象前缀目前安全兜底但不解析~~ **已在 1b-2 解决**(`youzi/llm/extract.py` 配平括号扫描,`parse_decision`/`parse_ops` 复用);已知限制:prose 中先出现配平 `{...}` 会被取(json_object 模式风险低)。
- 3 条种子非 canonical token(`次新/启动`、`连板/主升后期/情绪极值` 等)只留在 `regime_raw`,`for_regime` 查不到;Refiner/种子curation 修。
- 涌现技能审计闸(拦截"利用回测漏洞的作弊技能")、模拟成本 vs 实盘成本 diff 警报(蓝图 §6.3/§8)。

**Phase-1b-1 推迟项(随能力解锁,非阻塞)**
- **相位依赖签名**(退潮接力 `relay_in_ebb`)、任何需要离散相位的签名 → 待校准的 `G_cycle` 相位分类器(`MarketState` 现无离散相位字段,硬判=绝对阈值,违纪)。同理"按 regime 选择性注入提示"也被卡在此。
- **K线 / 持有退出签名**(黄昏之星/出货分时/该走不走)→ 待 OHLCV oracle / 持仓模型;1b-1 只覆盖**入场**类失败。
- "比同板更高板续板我却接杂毛"需对**全 universe**打分(当前仅决策集)。
- ~~Refiner **在线/流式增量信用**~~ **已在 1b-3a 落地**(`InnerLoop` 每个刚评分步 `apply_credit` 一次、就地更新 stats);EWMA decay 加权/regime 双衰减仍待六道闸。
- `SkillStats` 的 `expectancy` 是 Welford 累计均值,隐式依赖"n 与 expectancy 由 `apply_credit` 同步维护";若别处只改其一会失真,可在 1b-3 加一条往返回归钉死(终审 nit)。

**Phase-1b-2 推迟/债务(随能力解锁,非阻塞)**
- **ΔG live pass** 待 G 子 Agent 群建成(现占位 no-op);**涌现技能审计闸**(防 reward-hack 作弊技能,蓝图 §6.3/§8)未做。
- **编辑数上限默认值**(per-pass 5 / per-refine 12)为初值,实盘多日跑后按编辑质量调;**系统提示未把数值上限透传给 LLM**(只定性"少而精")→ 多产无效 op 被拒,1b-3 调参时补。
- **create 路径对注入的 stats/importance 静默剥离**(非显式 `rejected`)→ 功能安全但无审计可见性;若要审计 LLM 越权注入企图,可改为对含观测字段的 create op 也走 rejected。
- ~~**checkpoint-before / 能力地板熔断 / 何时触发 refine / 在线流式增量信用**~~ **已在 1b-3a 落地**(InnerLoop)。
- **按 regime 选择性提示注入** 仍卡在缺 `G_cycle` 相位分类器。

**Phase-1b-3a 推迟/债务(随能力解锁,非阻塞)**
- **能力地板熔断默认初值**(floor_abs −0.2 / margin 0.15 / window 20 / min_samples 40)为初值,实盘多日跑后调参。
- **rollback 到"上个 checkpoint"** 而非"退化窗口前 good 态"(多 refine 回退是增强);**每日 checkpoint = N 个磁盘快照**(~68KB/个,keep-last-K / 内存 ring 是增强)。
- **基线含早期 refine 效应**(baseline = 前 N 个已评分候选均值,期间 refine 已在跑;纯种子基线需 no-refine burn-in)。
- ~~**HCH/Hexpert/Hmin 三方度量对比**~~ **已在 1b-3b 落地**(`compare_harnesses`);**影子 Hexpert 严格地板** 仍待 1b-3c。

**Phase-1b-3b 推迟/债务(随能力解锁,非阻塞)**
- **影子 Hexpert 环内严格地板** = 1b-3c(并行冻结 agent 实时熔断;1b-3a/3b 用自相对地板 + 离线对比)。
- **按 regime 分层对比**(需 `G_cycle` 分类器)、**N 日/收益幅度 oracle + 成本/滑点**(需 OHLCV)、**多 episode/多窗口聚合对比**。
- **⚠ MockLLM 忽略提示 → 测不了 refine 实效**:真实"自进化是否胜 frozen / 是否退化到比 frozen 还差"(蓝图 §6.9)**必须真实 DeepSeek 多日跑**——先跑 `scripts/smoke_deepseek_agent.py`,再用 `compare_harnesses` 量。

---

## 6. 实盘 / 真实数据前 checklist
1. `python scripts/smoke_akshare.py 20240627` —— 核对 akshare 真实列名,修 `_RENAME`(P0)。
2. `DEEPSEEK_API_KEY=... python scripts/smoke_deepseek_agent.py 20240627` —— 验证 DeepSeek 真实输出可解析、选的 code 在候选池内(顺带再核 akshare)。
3. 解决 survivorship / PIT 数据治理(P0)。
4. 给 DeepSeekClient 加 retry;先**纸面/影子盘**跑 `WalkForwardEval`,确认成本/涨停买不进折算后仍有 edge,再小资金 HITL 灰度。
5. 守住蓝图边界:**Co-pilot,人确认下单,不自动下真单**;能力地板以下冻结自进化。

---

## 7. 关键文件 / 索引
- **设计**:`自进化游资系统-架构蓝图-v1.0.md`(11 节,主映射表/双环图/子系统/六道闸/术语表)。
- **状态**:`PROJECT_STATE.md`(一页纸,各阶段完成度 + 债务 §7)。
- **计划**(`docs/superpowers/plans/`,18 份):…fe0-web-foundation / feb-research-dashboard / **fea-decision-cockpit**。**设计 spec** 在 `docs/superpowers/specs/`(…FE-0、FE-B、**FE-A** 均有 spec+plan)。
- **web 层**(`youzi_web/`):FE-0 地基 + FE-B(research 3 视图)+ **FE-A**(`features/decision`/决策驾驶舱:离线渲染已存决策,市场态+候选卡+join技能计划)。领域 `youzi/` 零改、单向依赖。
- **web 层**(`youzi_web/`):FE-0 地基(registry/app/data_access/features/research H查看器)+ **FE-B**(data_access 读 run-store + research 3 视图 compare/refine/trajectory + 运行选择器)。run-store=`youzi/loop/run_store.py`。领域 `youzi/` 零改、单向依赖。
- **web 层**(`youzi_web/`,FE-0 起):`registry`(Feature/SubNavItem)· `app`(外壳C/create_app)· `data_access`(harness_view)· `features/research`(H 查看器)· `templates`/`static`。领域 `youzi/` 零改、单向依赖。
- **种子知识库**:`seeds/{skills,memory,doctrine,state_machine}.json` + `seeds/README.md`(schema + 相位归一词表 + v1 缺口);`seeds/_coverage_v1.md`(抽取覆盖度批判)。
- **源材料**:`轮回.docx`(游资 playbook)· `2605.09998v1.pdf`(Continual Harness 论文)。
- **手动验证**:`scripts/smoke_akshare.py`(核 akshare 列名)· `scripts/smoke_deepseek_agent.py`(真实单日 agent)· `scripts/smoke_compare.py <start> <end>`(**真实三方对比 + HCH 自进化轨迹**;窗口须在最近 ~30 交易日内,akshare 池数据限制)。
- **🔬 真实数据结论**:`docs/findings/2026-06-06-real-data-hch-vs-hexpert.md`(3/3 窗 HCH 退化于 frozen,§6.9 复现 + 退化机制 + 行动方向)。
- **跨会话记忆**:`~/.claude/projects/.../memory/youzi-self-evolving-project.md`(新会话自动加载的项目指针)。

---

### 给接手者的一句话
**自进化内环 + 度量机器全就绪。** `InnerLoop.run()` 把 act→打分→在线信用→refine 跑成 reset-free 交错环(+ 能力地板熔断);`compare_harnesses()` 把它放上同窗同 oracle 的尺,对比 HCH(自精炼)vs Hexpert(种子静态)vs Hmin(裸 baseline),出 `hch_beats_hexpert` verdict。底座(观测层/Refiner/内环/checkpoint·回滚/熔断/三方对比)全就绪,LLM-不可信边界经两轮 opus 对抗终审硬化。**🔬 真实数据已验(2026-06-06,三窗)**:3/3 窗 HCH 退化于 frozen(§6.9 复现),病因=Refiner 小样本过度退役(`docs/findings/2026-06-06-real-data-hch-vs-hexpert.md`)。**✅ 已治本 1b-3d**:退役证据门(n≥K)+ faded≠nuked 提示纪律,直接掐死"n=2~4 就退役"。**所以下一步最该先做的是:用 `scripts/smoke_compare.py` 再跑真实三窗,确认 1b-3d 是否把 HCH 拉回 ≥ Hexpert**(改进前 3/3 退化 vs 改进后,记进 `docs/findings/`)。之后再视结果上 ② 1b-3c 影子 Hexpert 严格地板(自相对/退役门抓不到"比 frozen 差",影子能抓)→ ③ 多窗口/regime 聚合定量结论 → 1c 协同学习外环。动手前重读第 2 节不变量、第 4 节路线图、findings,沿用第 3 节工作流;真实跑前先 smoke。
